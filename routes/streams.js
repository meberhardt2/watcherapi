/**************************************************************************************/
const streams = (app,DB,allowed_ip) => {

    /********************************************/
	app.get('/api/streams', async (request, response) => {

		response.json({
			status: 'success'
        });
	});	
    /********************************************/
	
}
/**************************************************************************************/

module.exports = streams;
